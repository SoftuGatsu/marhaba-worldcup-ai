# Demo Mode Configuration
# Set to 'true' to enable demo mode with predefined responses
# When enabled, all responses will be pulled from hardcoded demo scenarios
VITE_DEMO_MODE=true

# Agent Only Mode Configuration  
# Set to 'true' to use only agent services for responses (no hardcoded fallbacks)
VITE_AGENT_ONLY_MODE=true
