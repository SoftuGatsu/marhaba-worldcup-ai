# Marhaba World Cup AI Configuration

# Set to 'true' to use only agent services for responses (no hardcoded fallbacks)
# Set to 'false' or leave empty to allow fallback to real backend API when agents are unavailable
VITE_AGENT_ONLY_MODE=true
